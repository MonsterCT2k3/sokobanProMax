<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:background="@drawable/pixel_button_primary"
    android:orientation="vertical"
    android:padding="30dp">

    <!-- Title -->
    <TextView
        android:id="@+id/lose_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginBottom="15dp"
        android:text="💀 GAME OVER! 💀"
        android:textColor="@android:color/white"
        android:textSize="36sp"
        android:textStyle="bold" />

    <!-- Message -->
    <TextView
        android:id="@+id/lose_message"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginBottom="25dp"
        android:text="Bạn đã thua ở Level 1!"
        android:textColor="@android:color/white"
        android:textSize="18sp" />

    <!-- Buttons container -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:orientation="vertical"
        android:layout_marginTop="10dp">

        <!-- Retry Button -->
        <Button
            android:id="@+id/btn_retry"
            android:layout_width="180dp"
            android:layout_height="50dp"
            android:layout_marginBottom="10dp"
            android:background="@drawable/pixel_button_primary"
            android:text="CHƠI LẠI"
            android:textColor="@android:color/white"
            android:textSize="16sp"
            android:textStyle="bold" />

        <!-- Back to Level Selection Button -->
        <Button
            android:id="@+id/btn_back_to_level_selection"
            android:layout_width="180dp"
            android:layout_height="50dp"
            android:layout_marginBottom="10dp"
            android:background="@drawable/pixel_button_secondary"
            android:text="VỀ CHỌN LEVEL"
            android:textColor="@android:color/white"
            android:textSize="16sp"
            android:textStyle="bold" />

        <!-- Back to Menu Button -->
        <Button
            android:id="@+id/btn_back_to_menu"
            android:layout_width="180dp"
            android:layout_height="50dp"
            android:background="@drawable/pixel_button_secondary"
            android:text="VỀ MENU"
            android:textColor="@android:color/white"
            android:textSize="16sp"
            android:textStyle="bold" />

    </LinearLayout>

</LinearLayout>
